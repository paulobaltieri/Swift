/*
O Operador Ternário verifica uma condição e retorna um dentre dois valores pré-definidos em sua estrutura

*/
let text =
"""
 O Operador Ternário verifica uma condição e retorna um dentre dois valores pré-definidos em sua estrutura.

exemplo:

Nesse caso estamos verificando se a variavel idadePessoa é menor que a maiorIdade. Caso essa primeira condição seja atendida, teremos como resultado [Acesso negado], caso essa condição não seja atendida será exibida a proxima condição.

let maiorIdade = 18
let idadePessoa = 20
let permissao = idadePessoa < maiorIdade ? "Acesso negado" : "Acesso permitido"
print(permissao)

"""
print (text)

let maiorIdade = 18
let idadePessoa = 20
let permissao = idadePessoa < maiorIdade ? "Acesso negado" : "Acesso permitido"



 
  